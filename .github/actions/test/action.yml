name: 'Test'
description: 'Test using devcontainer'
runs:
  using: 'docker'
  image: '../../../.devcontainer/Dockerfile'
  entrypoint: "/github/workspace/.github/actions/test/run.sh"
  # entrypoint: ".github/actions/lint/run.sh"
  # entrypoint: bash
  # args: [ -lc, /github/workspace/.github/actions/lint/run.sh ]
