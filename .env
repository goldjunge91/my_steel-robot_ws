PYTHONPATH=src:install/lib/python3.10/site-packages
# my_steel robot - example environment variables for SBC (Raspberry Pi)
# Copy to .env and edit values for your robot before running just/setup scripts.

# Target platform: robot (SBC/Raspberry Pi) or remote_pc (development machine)
TARGET=robot

# ROS
ROS_DISTRO=humble

# Hardware model: robot_xl (only supported model)
ROBOT_MODEL_NAME=robot_xl

# Drive type: mecanum (only supported for robot_xl)
DRIVE_TYPE=mecanum

# Serial / microcontroller
SERIAL_PORT=/dev/ttyACM0
SERIAL_BAUDRATE=115200

# micro-ROS agent (true/false)
MICROROS=true

# Optional: manipulator serial (FTDI) - auto-detected if left blank
MANIPULATOR_SERIAL_PORT=/dev/ttyUSB0

# Optional: Pico SDK path used by firmware build scripts
PICO_SDK_PATH=/home/marco/development/micro_ros_ws/pico-sdk

# Camera (device or namespace) - leave blank to auto-detect
CAMERA_DEVICE=/dev/video0